{
  "name": "json8-pointer",
  "version": "0.2.0",
  "description": "JSON Pointer implementation for JavaScript",
  "keywords": [
    "JSON",
    "pointer"
  ],
  "homepage": "https://github.com/JSON8/pointer",
  "main": "index.js",
  "repository": "github:JSON8/pointer",
  "scripts": {
    "preversion": "npm run browserify && npm test",
    "browserify": "webpack -p index.js JSON8Pointer.js --output-library=JSON8Pointer",
    "lint": "eslint .",
    "unit": "mocha --compilers js:babel/register --recursive test/",
    "test": "npm run unit && npm run lint"
  },
  "author": "Sonny Piers <sonny@fastmail.net>",
  "license": "MIT",
  "dependencies": {
    "json8-core": "^0.2.0"
  }
}
