{
  "name": "json8-pointer",
  "version": "0.5.0",
  "description": "JSON Pointer implementation for JavaScript",
  "keywords": [
    "JSON",
    "pointer"
  ],
  "homepage": "https://github.com/JSON8/pointer",
  "repository": "github:JSON8/pointer",
  "bugs": "https://github.com/JSON8/merge-patch/issues",
  "main": "index.js",
  "scripts": {
    "preversion": "npm run webify && npm test",
    "webify": "browserify -s JSON8Pointer index.js -o JSON8Pointer.js",
    "lint": "./node_modules/.bin/eslint .",
    "unit": "mocha --compilers js:babel/register --recursive test/",
    "test": "npm run unit && npm run lint && npm run webify"
  },
  "author": "Sonny Piers <sonny@fastmail.net>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^0.24.1"
  }
}
